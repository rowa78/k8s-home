---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reise
  namespace: reise-blog
spec:
  rules:
  - host: reise.${SECRET_DOMAIN}
    http:
      paths:
      - backend:
          service: 
            name: wordpress
            port: 
              number: 80
        path: /
        pathType: Prefix
  tls:
    - hosts:
      - reise.${SECRET_DOMAIN}