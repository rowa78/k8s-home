apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: mariadb-secrets #this name will also be used for naming the generated kubernetes secret
  namespace: seat
spec:
  itemPath: "vaults/k8s/items/seat-mariadb-secret"